name: Build Android APK
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Install Dependencies
        run: npm install --force

      - name: Build Web Project
        run: |
          npm run build -- --base=./ || npm run build || true
          if [ ! -d "dist" ]; then
            mkdir -p dist
            cp index.html dist/ 2>/dev/null || true
          fi

      - name: Setup Capacitor Config # এই স্টেপটি মডিফাই করা হয়েছে
        run: |
          npm install @capacitor/core @capacitor/cli @capacitor/android @capacitor/assets
          cat <<EOF > capacitor.config.json
          {
            "appId": "com.ec.app",
            "appName": "GKS",
            "webDir": "dist",
            "server": {
              "androidScheme": "https",
              "hostname": "localhost",
              "cleartext": true
            }
          }
          EOF
          npx cap add android || true
          npx cap sync android
          
          # তৈরি করা আইকনটি দিয়ে অ্যাপের নেটিভ আইকন জেনারেট করা হচ্ছে
          mkdir -p resources
          cp public/icon.png resources/icon.png
          npx @capacitor/assets generate --android

      - name: Fix Gradle Version
        run: |
          cd android
          sed -i 's|gradle-.*-all.zip|gradle-8.13-all.zip|g' gradle/wrapper/gradle-wrapper.properties

      - name: Build Android APK
        run: |
          cd android
          echo "configurations.all {
              resolutionStrategy.eachDependency { DependencyResolveDetails details ->
                  if (details.requested.group == 'org.jetbrains.kotlin') {
                      details.useVersion '1.8.22'
                  }
              }
          }" >> app/build.gradle
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Mobile-App-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
